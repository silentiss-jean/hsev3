/**
 * overview_view.js — Onglet Vue d'ensemble HSE V3
 * STUB — à implémenter (étape 2)
 */
export class OverviewView {
  mount(el, ctx) {
    el.innerHTML = '<div style="padding:2rem;color:var(--hse-text-muted,#6b7280)">Vue d\'ensemble — en cours de développement.</div>';
  }
  unmount() {}
}
