/**
 * config_view.js — Onglet Configuration HSE V3
 * STUB — à implémenter (étape 3)
 */
export class ConfigView {
  mount(el, ctx) {
    el.innerHTML = '<div style="padding:2rem;color:var(--hse-text-muted,#6b7280)">Configuration — en cours de développement.</div>';
  }
  unmount() {}
}
